<h2 class="heading">All Recipes...</h2>
<p>
  <% if user_signed_in? %>
    <%= button_to "Add New Recipe", new_recipe_path, method: :get, class:"add_btn" %>
  <% else %>
    <h3>Sign in to view and create your recipes</h3>
  <% end %>
</p>

<% if @recipes.any? %> 
  <ul class="recipe_list">
    <% @recipes.each do |recipe| %>
      <li class="recipe_container">
        <div class="recipe_name">
          <%= link_to recipe.name, recipe_path(recipe) %>
          <span>
            <% if user_signed_in? %>
              <%= button_to "Remove", recipe_path(recipe), method: :delete, class: "btn"  %>
            <% end %>
          </span>
        </div>
        <div class="recipe_description">
          <%= recipe.description %>
        </div>
      </li>
    <% end %>
  </ul> 
<% else %>
  <% if user_signed_in? %>
    <div>
      <h3>😢You don't have any recipes saved. Create a recipe to see it here😪</h3>
    </div>
  <% end %>
<% end %>
